import { Close } from "flowbite-react-icons/outline";

export default function ButtonClose() {
  return (
    <button onClick={() => {}} className="cursor-pointer text-gray-400 hover:text-white transition-colors">
      <Close size={28} />
    </button>
  )
}